globals = {
    loglevel = 6;
    syslog = true;
    verbose = true;
};

suricatta = {
    server = "hawkbit";
    url = "https://localhost";
    nocheckcert = true;
    polldelay = 10;
    tenant = "DEFAULT";
};

download = {
    retries = 3;
    timeout = 1800;
};
